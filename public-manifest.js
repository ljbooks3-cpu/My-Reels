(()=>{'use strict';
/* Public viewer bootstrap. This intentionally contains no Google OAuth. The public
   catalogue is populated by the creator-published manifest when available. */
const MANIFEST_URL='';
window.MyReelsPublic={manifestUrl:MANIFEST_URL};
})();